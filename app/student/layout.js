export const metadata = {
  title: 'Eventify - Campus Events',
  description: 'Discover upcoming events at your campus',
};

export default function StudentLayout({ children }) {
  return <>{children}</>;
}
